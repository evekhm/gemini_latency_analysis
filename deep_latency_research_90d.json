{
    "state": {},
    "kpis": {
        "mean_latency_target": 3.0
    },
    "num_slowest_queries": 20,
    "queries": [
        "Perform a comprehensive deep research analysis of latency patterns for the last 90 days. First, check compliance against the project KPIs (mean latency < 3s). Then, test all hypotheses systematically: 1) Token size drives latency, 2) Agent-specific performance issues, 3) Time-based patterns, 4) Clustering patterns, 5) Outlier behavior, and 6) Request Queuing (latency spikes due to micro-bursts). For the queuing hypothesis, specifically analyze if latency correlates with request bursts in 1-second windows. Fetch and analyze the slowest queries (use the num_slowest_queries parameter from config) to understand root causes. Provide statistical evidence for all findings. At the end, provide a prioritized list of actionable recommendations."
    ]
}